#include <bits/stdc++.h>
using namespace std;

int fun(int a, int b)
{
}

int fun2(int p, int q)
{
}

int fun3(int a, int b, int c)
{
    c = 21 + b;
    a = 3 & c;
    b = (2 & 12) + c;
    c = a + c;
    b = (2 & 12) + c;
    return a + b + c;
}
int fun4(int p, int q, int r)
{
}
int main()
{
    // cout << fun(5, 5);
    // cout << fun2(5, 5);
    // cout << "fun3: " << fun3(4, 5, 6);
    // cout << "fun4: " << fun3(4, 5, 6);

    int a = 6, b = 8, c = 8;
    int p = 0, q = 4, r = 4;
    for (r = 5; r <= 8; r++)
    {
        }
    cout << p + q;

    // cout << a + b;
    // cout << p + q;
    //  cout << a + b + c;
    // cout << p + q + r;
}